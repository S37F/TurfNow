# TurfNow — frontend (Vite)
# Copy to .env in this folder (e.g. copy .env.example .env)

# Dev: Vite proxies /api → Express on port 5000 (vite.config.js)
VITE_API_URL=/api

# If the UI and API are on different origins, set the API base URL, e.g.:
# VITE_API_URL=https://api.example.com/api
